[project]
name = "anthropic-api-introduction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rimaz Azeez", email = "razeez@cleverbrooks.com" }
]
requires-python = ">=3.13"
dependencies = [
    "anthropic[bedrock]>=0.121.0",
    "ipykernel>=7.3.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
anthropic-api-introduction = "anthropic_api_introduction:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
